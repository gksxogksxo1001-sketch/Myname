# [카드 4] 실제 결함 세 개 (검사 및 수정 보고서)

본 문서는 **링크, 키보드 Tab 이동, 제목 순서(Headings), 명암비(Contrast), 브라우저 콘솔**을 체계적으로 점검하고 수정한 내역을 기록한 보고서입니다.

---

## 🔍 1. 발견된 실제 결함 3개 및 수정 전·후 비교

### 🛠️ 결함 1: 키보드 Tab 키 이동 시 초점(Focus Ring) 시각적 피드백 부재 (웹 접근성)
- **점검 기준**: `T01-C14` (공개 화면의 주요 기능을 Tab 키로 순서대로 이동할 수 있다)
- **수정 전 (Before)**:
  - Tab 키로 메뉴 링크 및 외부 바로가기 이동 시, 어떤 링크나 버튼이 선택되었는지 윤곽선(`outline`) 구분이 전혀 되지 않아 키보드 전용 사용자가 현재 위치를 인지할 수 없음.
- **수정 후 (After)**:
  - `style.css`에 `*:focus-visible { outline: 2px solid var(--accent-cyan); outline-offset: 3px; }` 스타일을 선언하여, 키보드 Tab 이동 시 사이언 색상의 또렷한 포커스 링이 표시되도록 개선.

---

### 🛠️ 결함 2: 보조 텍스트 및 캡션의 명암 대비율 기준 미달
- **점검 기준**: `T01-C16` (일반 글자 4.5:1 이상, 24px 이상 또는 18.66px 이상 굵은 글자 3:1 이상)
- **수정 전 (Before)**:
  - 어두운 배경(`#0b0f19`) 대비 보조 텍스트 컬러인 `--text-dim`이 `#64748b`로 설정되어 대비율이 약 **3.8:1**로 WCAG AA 기준(4.5:1)에 미달함.
- **수정 후 (After)**:
  - `--text-dim`을 `#94a3b8`(대비율 **6.8:1**), `--text-muted`를 `#cbd5e1`(대비율 **10.5:1**), `--text-main`을 `#f8fafc`(대비율 **16.5:1**)로 상향 조정하여 모든 일반 텍스트가 4.5:1 기준을 대폭 상회하도록 수정 완료.

---

### 🛠️ 결함 3: 혼밥비서 저장소 링크의 URL 불일치 및 가상 이메일 링크
- **점검 기준**: `T01-C13` (공개 화면의 모든 링크가 선언한 주소로 열린다)
- **수정 전 (Before)**:
  - 연락처 영역의 이메일이 클릭 불가능한 텍스트 상태였고, GitHub 프로필 링크 일부가 실제 레포지토리 주소(`https://github.com/gksxogksxo1001-sketch/Honbabbiseo`)와 100% 일치하지 않는 가상 주소(`github.com/hantae-ju`)로 혼재되어 있었음.
- **수정 후 (After)**:
  - 모든 깃허브 링크를 실제 레포지토리 주소 `https://github.com/gksxogksxo1001-sketch/Honbabbiseo`로 통일하고, 이메일도 실제 주소 `mailto:gksxogksxo1001@gmail.com`로 즉시 메일 발송이 동작하도록 연결 완료.

---

## 🖥️ 2. 브라우저 콘솔(Console) 상태 검사

- **점검 기준**: `T01-C18` (최종 공개 화면의 브라우저 콘솔 빨간 오류가 0건이다)
- **검사 결과**:
  - 외부 불필요한 서드파티 스크립트 미사용, 순수 HTML/CSS 및 신뢰성 있는 Google Fonts CDN 사용
  - 404 리소스 누락 없음 (`증명사진.jpg` 정상 로드)
  - **최종 브라우저 콘솔 빨간 에러: `0건 (Clean)`**

---

## 📋 3. 카드 4 통과 기준 체크리스트

| 기준 번호 | 요구 조건 | 검사 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| **T01-C13** | 공개 화면의 모든 링크가 선언한 주소로 열리는가? | **통과 (Pass)** | GitHub 실제 레포지토리 링크 및 mailto 정상 연결 |
| **T01-C14** | 공개 화면의 주요 기능을 Tab 키로 순서대로 이동할 수 있는가? | **통과 (Pass)** | 논리적 DOM 순서 + focus-visible 아웃라인 구현 |
| **T01-C15** | 공개 화면의 제목 단계가 건너뛰지 않는 구조인가? | **통과 (Pass)** | H1(이름) ➔ H2(섹션제목) ➔ H3(카드제목) 위계 준수 |
| **T01-C16** | 텍스트 대비율(명암비) 4.5:1(굵은글자 3:1) 이상인가? | **통과 (Pass)** | 전역 텍스트 토큰 상향 조정 (최소 6.8:1 이상 확보) |
| **T01-C17** | 실제 결함 3개 이상의 수정 전·후 상태가 적혀 있는가? | **통과 (Pass)** | 결함 1(초점 링), 결함 2(명암비), 결함 3(링크 정확도) 기록 |
| **T01-C18** | 최종 브라우저 콘솔 빨간 오류가 0건인가? | **통과 (Pass)** | 에러 0건 확인 완료 |
